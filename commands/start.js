const game = require('../src/PromptGame');
module.exports = {
	name: 'start',
	description: 'sends a start message',
	execute(message) {
		return new Promise( (resolve, reject) => {
			//ToDo: Start Method
		});
	},
}